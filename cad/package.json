{
  "name": "foto3d-cad",
  "private": true,
  "type": "module",
  "description": "Kernel CAD del repo (three.js). npm i para correr tests y exportadores desde node.",
  "scripts": {
    "test": "for f in tests/test_*.mjs; do node $f || exit 1; done",
    "render": "node ensambles/nbt90/render.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "meshoptimizer": "^1.2.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38",
    "playwright": "^1.62.0"
  }
}
